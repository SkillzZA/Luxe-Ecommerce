# Environment variables for Luxe Ecommerce

# --- Database --- 
# For local development with SQLite (default):
# DATABASE_URL="file:./dev.db"
# For production with Neon (or other PostgreSQL):
DATABASE_URL="YOUR_PRODUCTION_POSTGRESQL_URL_HERE"

# --- Authentication --- 
# Secret for signing custom JWTs (used in lib/auth.ts)
# Generate with: openssl rand -base64 32
JWT_SECRET="your_strong_jwt_secret_here"

# --- NextAuth.js --- 
# The canonical URL of your deployed application (e.g., http://localhost:3000 for local, https://your-site.netlify.app for production)
NEXTAUTH_URL="http://localhost:3000"

# Secret for NextAuth.js to sign cookies, JWTs, etc.
# Generate with: openssl rand -base64 32 (must be different from JWT_SECRET)
NEXTAUTH_SECRET="your_strong_nextauth_secret_here"

# Optional: If using specific OAuth providers with NextAuth.js, their credentials would go here
# GITHUB_ID=""
# GITHUB_SECRET=""
# GOOGLE_CLIENT_ID=""
# GOOGLE_CLIENT_SECRET="" 